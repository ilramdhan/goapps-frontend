// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: finance/v1/cost_product_param_bulk.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BaseResponse } from "../../common/v1/common";

export const protobufPackage = "finance.v1";

/** AddApplicableParamOp marks a parameter as applicable to every targeted product. */
export interface AddApplicableParamOp {
  paramId: string;
  isRequired: boolean;
  /** 0 means inherit mst_parameter.display_order. */
  displayOrder: number;
}

/**
 * RemoveApplicableParamOp removes a parameter (and its value) from every
 * targeted product it currently applies to.
 */
export interface RemoveApplicableParamOp {
  paramId: string;
}

/**
 * UpsertParamValueOp sets a param value on every targeted product that has
 * the parameter applicable — see BulkEditProductParamsRequest.skip_missing_applicable
 * for the behavior when a targeted product does not yet have it applicable.
 */
export interface UpsertParamValueOp {
  paramId: string;
  /** Value as string (decimals as text for precision). One of these must be set. */
  valueNumeric: string;
  valueText: string;
  /**
   * value_flag is a real bool — we cannot distinguish "unset" from FALSE, so
   * callers must use has_value_flag to opt in.
   */
  valueFlag: boolean;
  hasValueFlag: boolean;
}

/** BulkParamOperation is one step applied, in order, to every targeted product. */
export interface BulkParamOperation {
  addApplicable?: AddApplicableParamOp | undefined;
  removeApplicable?: RemoveApplicableParamOp | undefined;
  upsertValue?: UpsertParamValueOp | undefined;
}

/**
 * BulkEditProductParamsRequest applies a list of param operations, in order,
 * to a batch of products in one call.
 */
export interface BulkEditProductParamsRequest {
  /** cost_product_master.cpm_product_sys_id values to edit. 1-500 items. */
  productSysIds: number[];
  /** Operations applied in order to every targeted product. */
  operations: BulkParamOperation[];
  /**
   * When TRUE (default), an UpsertParamValueOp targeting a product where the
   * param is not yet applicable is skipped and reported as a failure for that
   * product rather than silently auto-adding the applicability row first.
   */
  skipMissingApplicable: boolean;
}

/**
 * BulkEditProductParamsResponse is the response for queuing a bulk param-edit
 * job. The job runs asynchronously; poll GetBulkProductParamJobStatus for progress.
 */
export interface BulkEditProductParamsResponse {
  /** Standard response metadata. */
  base:
    | BaseResponse
    | undefined;
  /** Queued job summary (job_id, job_code, status, progress counters). */
  data: BulkParamJobInfo | undefined;
}

/** BulkParamJobInfo summarizes a freshly-queued bulk product-param edit job. */
export interface BulkParamJobInfo {
  /** job_execution.job_id (UUID). */
  jobId: string;
  /** Human-readable code (e.g. "CPP_BULK-202609-001"). */
  jobCode: string;
  /** Initial status, typically "QUEUED". */
  status: string;
  /** Total products expected to be processed by this job. */
  totalChildren: number;
  /** Products processed successfully so far. */
  completedChildren: number;
  /** Products that failed so far. */
  failedChildren: number;
}

/**
 * GetBulkProductParamJobStatusRequest identifies the bulk product-param edit
 * job whose live status/progress should be polled while it is still running.
 */
export interface GetBulkProductParamJobStatusRequest {
  /** job_execution.job_id (UUID). */
  jobId: string;
}

/**
 * GetBulkProductParamJobStatusResponse reports the bulk job's current status
 * and live child-completion counters.
 */
export interface GetBulkProductParamJobStatusResponse {
  /** Standard response metadata. */
  base:
    | BaseResponse
    | undefined;
  /** job_execution.job_id (UUID). */
  jobId: string;
  /** Human-readable code (e.g. "CPP_BULK-202609-001"). */
  jobCode: string;
  /** Current status (e.g. "QUEUED", "PROCESSING", "DONE", "FAILED", "PARTIAL"). */
  status: string;
  /** Total products expected to be processed by this job. */
  totalChildren: number;
  /** Products processed successfully so far. */
  completedChildren: number;
  /** Products that failed so far. */
  failedChildren: number;
}

/**
 * ListBulkProductParamJobFailuresRequest identifies the bulk product-param
 * edit job whose per-item failures should be listed.
 */
export interface ListBulkProductParamJobFailuresRequest {
  /** job_execution.job_id (UUID). */
  jobId: string;
}

/**
 * ListBulkProductParamJobFailuresResponse lists every product that failed
 * within a bulk job, with its error message.
 */
export interface ListBulkProductParamJobFailuresResponse {
  /** Standard response metadata. */
  base:
    | BaseResponse
    | undefined;
  /** Failed items, in the order the worker processed them. */
  failures: BulkProductParamJobFailure[];
}

/**
 * BulkProductParamJobFailure describes one product that failed within a bulk
 * param-edit job.
 */
export interface BulkProductParamJobFailure {
  /** cost_product_master.cpm_product_sys_id that failed. */
  productSysId: number;
  /** Product's business code, for display without a follow-up lookup. */
  productCode: string;
  /** Human-readable error message explaining the failure. */
  errorMessage: string;
}

function createBaseAddApplicableParamOp(): AddApplicableParamOp {
  return { paramId: "", isRequired: false, displayOrder: 0 };
}

export const AddApplicableParamOp: MessageFns<AddApplicableParamOp> = {
  encode(message: AddApplicableParamOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paramId !== "") {
      writer.uint32(10).string(message.paramId);
    }
    if (message.isRequired !== false) {
      writer.uint32(16).bool(message.isRequired);
    }
    if (message.displayOrder !== 0) {
      writer.uint32(24).int32(message.displayOrder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddApplicableParamOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddApplicableParamOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paramId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isRequired = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.displayOrder = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddApplicableParamOp {
    return {
      paramId: isSet(object.paramId)
        ? globalThis.String(object.paramId)
        : isSet(object.param_id)
        ? globalThis.String(object.param_id)
        : "",
      isRequired: isSet(object.isRequired)
        ? globalThis.Boolean(object.isRequired)
        : isSet(object.is_required)
        ? globalThis.Boolean(object.is_required)
        : false,
      displayOrder: isSet(object.displayOrder)
        ? globalThis.Number(object.displayOrder)
        : isSet(object.display_order)
        ? globalThis.Number(object.display_order)
        : 0,
    };
  },

  toJSON(message: AddApplicableParamOp): unknown {
    const obj: any = {};
    if (message.paramId !== "") {
      obj.paramId = message.paramId;
    }
    if (message.isRequired !== false) {
      obj.isRequired = message.isRequired;
    }
    if (message.displayOrder !== 0) {
      obj.displayOrder = Math.round(message.displayOrder);
    }
    return obj;
  },

  create(base?: DeepPartial<AddApplicableParamOp>): AddApplicableParamOp {
    return AddApplicableParamOp.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddApplicableParamOp>): AddApplicableParamOp {
    const message = createBaseAddApplicableParamOp();
    message.paramId = object.paramId ?? "";
    message.isRequired = object.isRequired ?? false;
    message.displayOrder = object.displayOrder ?? 0;
    return message;
  },
};

function createBaseRemoveApplicableParamOp(): RemoveApplicableParamOp {
  return { paramId: "" };
}

export const RemoveApplicableParamOp: MessageFns<RemoveApplicableParamOp> = {
  encode(message: RemoveApplicableParamOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paramId !== "") {
      writer.uint32(10).string(message.paramId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveApplicableParamOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveApplicableParamOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paramId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveApplicableParamOp {
    return {
      paramId: isSet(object.paramId)
        ? globalThis.String(object.paramId)
        : isSet(object.param_id)
        ? globalThis.String(object.param_id)
        : "",
    };
  },

  toJSON(message: RemoveApplicableParamOp): unknown {
    const obj: any = {};
    if (message.paramId !== "") {
      obj.paramId = message.paramId;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveApplicableParamOp>): RemoveApplicableParamOp {
    return RemoveApplicableParamOp.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveApplicableParamOp>): RemoveApplicableParamOp {
    const message = createBaseRemoveApplicableParamOp();
    message.paramId = object.paramId ?? "";
    return message;
  },
};

function createBaseUpsertParamValueOp(): UpsertParamValueOp {
  return { paramId: "", valueNumeric: "", valueText: "", valueFlag: false, hasValueFlag: false };
}

export const UpsertParamValueOp: MessageFns<UpsertParamValueOp> = {
  encode(message: UpsertParamValueOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paramId !== "") {
      writer.uint32(10).string(message.paramId);
    }
    if (message.valueNumeric !== "") {
      writer.uint32(18).string(message.valueNumeric);
    }
    if (message.valueText !== "") {
      writer.uint32(26).string(message.valueText);
    }
    if (message.valueFlag !== false) {
      writer.uint32(32).bool(message.valueFlag);
    }
    if (message.hasValueFlag !== false) {
      writer.uint32(40).bool(message.hasValueFlag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpsertParamValueOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpsertParamValueOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paramId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.valueNumeric = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.valueText = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.valueFlag = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hasValueFlag = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpsertParamValueOp {
    return {
      paramId: isSet(object.paramId)
        ? globalThis.String(object.paramId)
        : isSet(object.param_id)
        ? globalThis.String(object.param_id)
        : "",
      valueNumeric: isSet(object.valueNumeric)
        ? globalThis.String(object.valueNumeric)
        : isSet(object.value_numeric)
        ? globalThis.String(object.value_numeric)
        : "",
      valueText: isSet(object.valueText)
        ? globalThis.String(object.valueText)
        : isSet(object.value_text)
        ? globalThis.String(object.value_text)
        : "",
      valueFlag: isSet(object.valueFlag)
        ? globalThis.Boolean(object.valueFlag)
        : isSet(object.value_flag)
        ? globalThis.Boolean(object.value_flag)
        : false,
      hasValueFlag: isSet(object.hasValueFlag)
        ? globalThis.Boolean(object.hasValueFlag)
        : isSet(object.has_value_flag)
        ? globalThis.Boolean(object.has_value_flag)
        : false,
    };
  },

  toJSON(message: UpsertParamValueOp): unknown {
    const obj: any = {};
    if (message.paramId !== "") {
      obj.paramId = message.paramId;
    }
    if (message.valueNumeric !== "") {
      obj.valueNumeric = message.valueNumeric;
    }
    if (message.valueText !== "") {
      obj.valueText = message.valueText;
    }
    if (message.valueFlag !== false) {
      obj.valueFlag = message.valueFlag;
    }
    if (message.hasValueFlag !== false) {
      obj.hasValueFlag = message.hasValueFlag;
    }
    return obj;
  },

  create(base?: DeepPartial<UpsertParamValueOp>): UpsertParamValueOp {
    return UpsertParamValueOp.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpsertParamValueOp>): UpsertParamValueOp {
    const message = createBaseUpsertParamValueOp();
    message.paramId = object.paramId ?? "";
    message.valueNumeric = object.valueNumeric ?? "";
    message.valueText = object.valueText ?? "";
    message.valueFlag = object.valueFlag ?? false;
    message.hasValueFlag = object.hasValueFlag ?? false;
    return message;
  },
};

function createBaseBulkParamOperation(): BulkParamOperation {
  return { addApplicable: undefined, removeApplicable: undefined, upsertValue: undefined };
}

export const BulkParamOperation: MessageFns<BulkParamOperation> = {
  encode(message: BulkParamOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.addApplicable !== undefined) {
      AddApplicableParamOp.encode(message.addApplicable, writer.uint32(10).fork()).join();
    }
    if (message.removeApplicable !== undefined) {
      RemoveApplicableParamOp.encode(message.removeApplicable, writer.uint32(18).fork()).join();
    }
    if (message.upsertValue !== undefined) {
      UpsertParamValueOp.encode(message.upsertValue, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkParamOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkParamOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.addApplicable = AddApplicableParamOp.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.removeApplicable = RemoveApplicableParamOp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.upsertValue = UpsertParamValueOp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkParamOperation {
    return {
      addApplicable: isSet(object.addApplicable)
        ? AddApplicableParamOp.fromJSON(object.addApplicable)
        : isSet(object.add_applicable)
        ? AddApplicableParamOp.fromJSON(object.add_applicable)
        : undefined,
      removeApplicable: isSet(object.removeApplicable)
        ? RemoveApplicableParamOp.fromJSON(object.removeApplicable)
        : isSet(object.remove_applicable)
        ? RemoveApplicableParamOp.fromJSON(object.remove_applicable)
        : undefined,
      upsertValue: isSet(object.upsertValue)
        ? UpsertParamValueOp.fromJSON(object.upsertValue)
        : isSet(object.upsert_value)
        ? UpsertParamValueOp.fromJSON(object.upsert_value)
        : undefined,
    };
  },

  toJSON(message: BulkParamOperation): unknown {
    const obj: any = {};
    if (message.addApplicable !== undefined) {
      obj.addApplicable = AddApplicableParamOp.toJSON(message.addApplicable);
    }
    if (message.removeApplicable !== undefined) {
      obj.removeApplicable = RemoveApplicableParamOp.toJSON(message.removeApplicable);
    }
    if (message.upsertValue !== undefined) {
      obj.upsertValue = UpsertParamValueOp.toJSON(message.upsertValue);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkParamOperation>): BulkParamOperation {
    return BulkParamOperation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkParamOperation>): BulkParamOperation {
    const message = createBaseBulkParamOperation();
    message.addApplicable = (object.addApplicable !== undefined && object.addApplicable !== null)
      ? AddApplicableParamOp.fromPartial(object.addApplicable)
      : undefined;
    message.removeApplicable = (object.removeApplicable !== undefined && object.removeApplicable !== null)
      ? RemoveApplicableParamOp.fromPartial(object.removeApplicable)
      : undefined;
    message.upsertValue = (object.upsertValue !== undefined && object.upsertValue !== null)
      ? UpsertParamValueOp.fromPartial(object.upsertValue)
      : undefined;
    return message;
  },
};

function createBaseBulkEditProductParamsRequest(): BulkEditProductParamsRequest {
  return { productSysIds: [], operations: [], skipMissingApplicable: false };
}

export const BulkEditProductParamsRequest: MessageFns<BulkEditProductParamsRequest> = {
  encode(message: BulkEditProductParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.productSysIds) {
      writer.uint32(8).int64(v!);
    }
    for (const v of message.operations) {
      BulkParamOperation.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.skipMissingApplicable !== false) {
      writer.uint32(24).bool(message.skipMissingApplicable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkEditProductParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkEditProductParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.productSysIds.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.productSysIds.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operations.push(BulkParamOperation.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.skipMissingApplicable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkEditProductParamsRequest {
    return {
      productSysIds: globalThis.Array.isArray(object?.productSysIds)
        ? object.productSysIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.product_sys_ids)
        ? object.product_sys_ids.map((e: any) => globalThis.Number(e))
        : [],
      operations: globalThis.Array.isArray(object?.operations)
        ? object.operations.map((e: any) => BulkParamOperation.fromJSON(e))
        : [],
      skipMissingApplicable: isSet(object.skipMissingApplicable)
        ? globalThis.Boolean(object.skipMissingApplicable)
        : isSet(object.skip_missing_applicable)
        ? globalThis.Boolean(object.skip_missing_applicable)
        : false,
    };
  },

  toJSON(message: BulkEditProductParamsRequest): unknown {
    const obj: any = {};
    if (message.productSysIds?.length) {
      obj.productSysIds = message.productSysIds.map((e) => Math.round(e));
    }
    if (message.operations?.length) {
      obj.operations = message.operations.map((e) => BulkParamOperation.toJSON(e));
    }
    if (message.skipMissingApplicable !== false) {
      obj.skipMissingApplicable = message.skipMissingApplicable;
    }
    return obj;
  },

  create(base?: DeepPartial<BulkEditProductParamsRequest>): BulkEditProductParamsRequest {
    return BulkEditProductParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkEditProductParamsRequest>): BulkEditProductParamsRequest {
    const message = createBaseBulkEditProductParamsRequest();
    message.productSysIds = object.productSysIds?.map((e) => e) || [];
    message.operations = object.operations?.map((e) => BulkParamOperation.fromPartial(e)) || [];
    message.skipMissingApplicable = object.skipMissingApplicable ?? false;
    return message;
  },
};

function createBaseBulkEditProductParamsResponse(): BulkEditProductParamsResponse {
  return { base: undefined, data: undefined };
}

export const BulkEditProductParamsResponse: MessageFns<BulkEditProductParamsResponse> = {
  encode(message: BulkEditProductParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      BulkParamJobInfo.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkEditProductParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkEditProductParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = BulkParamJobInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkEditProductParamsResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? BulkParamJobInfo.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: BulkEditProductParamsResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = BulkParamJobInfo.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkEditProductParamsResponse>): BulkEditProductParamsResponse {
    return BulkEditProductParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkEditProductParamsResponse>): BulkEditProductParamsResponse {
    const message = createBaseBulkEditProductParamsResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? BulkParamJobInfo.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseBulkParamJobInfo(): BulkParamJobInfo {
  return { jobId: "", jobCode: "", status: "", totalChildren: 0, completedChildren: 0, failedChildren: 0 };
}

export const BulkParamJobInfo: MessageFns<BulkParamJobInfo> = {
  encode(message: BulkParamJobInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.jobCode !== "") {
      writer.uint32(18).string(message.jobCode);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.totalChildren !== 0) {
      writer.uint32(32).int32(message.totalChildren);
    }
    if (message.completedChildren !== 0) {
      writer.uint32(40).int32(message.completedChildren);
    }
    if (message.failedChildren !== 0) {
      writer.uint32(48).int32(message.failedChildren);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkParamJobInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkParamJobInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalChildren = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.completedChildren = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.failedChildren = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkParamJobInfo {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      jobCode: isSet(object.jobCode)
        ? globalThis.String(object.jobCode)
        : isSet(object.job_code)
        ? globalThis.String(object.job_code)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      totalChildren: isSet(object.totalChildren)
        ? globalThis.Number(object.totalChildren)
        : isSet(object.total_children)
        ? globalThis.Number(object.total_children)
        : 0,
      completedChildren: isSet(object.completedChildren)
        ? globalThis.Number(object.completedChildren)
        : isSet(object.completed_children)
        ? globalThis.Number(object.completed_children)
        : 0,
      failedChildren: isSet(object.failedChildren)
        ? globalThis.Number(object.failedChildren)
        : isSet(object.failed_children)
        ? globalThis.Number(object.failed_children)
        : 0,
    };
  },

  toJSON(message: BulkParamJobInfo): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.jobCode !== "") {
      obj.jobCode = message.jobCode;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.totalChildren !== 0) {
      obj.totalChildren = Math.round(message.totalChildren);
    }
    if (message.completedChildren !== 0) {
      obj.completedChildren = Math.round(message.completedChildren);
    }
    if (message.failedChildren !== 0) {
      obj.failedChildren = Math.round(message.failedChildren);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkParamJobInfo>): BulkParamJobInfo {
    return BulkParamJobInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkParamJobInfo>): BulkParamJobInfo {
    const message = createBaseBulkParamJobInfo();
    message.jobId = object.jobId ?? "";
    message.jobCode = object.jobCode ?? "";
    message.status = object.status ?? "";
    message.totalChildren = object.totalChildren ?? 0;
    message.completedChildren = object.completedChildren ?? 0;
    message.failedChildren = object.failedChildren ?? 0;
    return message;
  },
};

function createBaseGetBulkProductParamJobStatusRequest(): GetBulkProductParamJobStatusRequest {
  return { jobId: "" };
}

export const GetBulkProductParamJobStatusRequest: MessageFns<GetBulkProductParamJobStatusRequest> = {
  encode(message: GetBulkProductParamJobStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBulkProductParamJobStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBulkProductParamJobStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBulkProductParamJobStatusRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
    };
  },

  toJSON(message: GetBulkProductParamJobStatusRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBulkProductParamJobStatusRequest>): GetBulkProductParamJobStatusRequest {
    return GetBulkProductParamJobStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBulkProductParamJobStatusRequest>): GetBulkProductParamJobStatusRequest {
    const message = createBaseGetBulkProductParamJobStatusRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseGetBulkProductParamJobStatusResponse(): GetBulkProductParamJobStatusResponse {
  return {
    base: undefined,
    jobId: "",
    jobCode: "",
    status: "",
    totalChildren: 0,
    completedChildren: 0,
    failedChildren: 0,
  };
}

export const GetBulkProductParamJobStatusResponse: MessageFns<GetBulkProductParamJobStatusResponse> = {
  encode(message: GetBulkProductParamJobStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.jobCode !== "") {
      writer.uint32(26).string(message.jobCode);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.totalChildren !== 0) {
      writer.uint32(40).int32(message.totalChildren);
    }
    if (message.completedChildren !== 0) {
      writer.uint32(48).int32(message.completedChildren);
    }
    if (message.failedChildren !== 0) {
      writer.uint32(56).int32(message.failedChildren);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBulkProductParamJobStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBulkProductParamJobStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobCode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalChildren = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.completedChildren = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.failedChildren = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBulkProductParamJobStatusResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      jobCode: isSet(object.jobCode)
        ? globalThis.String(object.jobCode)
        : isSet(object.job_code)
        ? globalThis.String(object.job_code)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      totalChildren: isSet(object.totalChildren)
        ? globalThis.Number(object.totalChildren)
        : isSet(object.total_children)
        ? globalThis.Number(object.total_children)
        : 0,
      completedChildren: isSet(object.completedChildren)
        ? globalThis.Number(object.completedChildren)
        : isSet(object.completed_children)
        ? globalThis.Number(object.completed_children)
        : 0,
      failedChildren: isSet(object.failedChildren)
        ? globalThis.Number(object.failedChildren)
        : isSet(object.failed_children)
        ? globalThis.Number(object.failed_children)
        : 0,
    };
  },

  toJSON(message: GetBulkProductParamJobStatusResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.jobCode !== "") {
      obj.jobCode = message.jobCode;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.totalChildren !== 0) {
      obj.totalChildren = Math.round(message.totalChildren);
    }
    if (message.completedChildren !== 0) {
      obj.completedChildren = Math.round(message.completedChildren);
    }
    if (message.failedChildren !== 0) {
      obj.failedChildren = Math.round(message.failedChildren);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBulkProductParamJobStatusResponse>): GetBulkProductParamJobStatusResponse {
    return GetBulkProductParamJobStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBulkProductParamJobStatusResponse>): GetBulkProductParamJobStatusResponse {
    const message = createBaseGetBulkProductParamJobStatusResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.jobId = object.jobId ?? "";
    message.jobCode = object.jobCode ?? "";
    message.status = object.status ?? "";
    message.totalChildren = object.totalChildren ?? 0;
    message.completedChildren = object.completedChildren ?? 0;
    message.failedChildren = object.failedChildren ?? 0;
    return message;
  },
};

function createBaseListBulkProductParamJobFailuresRequest(): ListBulkProductParamJobFailuresRequest {
  return { jobId: "" };
}

export const ListBulkProductParamJobFailuresRequest: MessageFns<ListBulkProductParamJobFailuresRequest> = {
  encode(message: ListBulkProductParamJobFailuresRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBulkProductParamJobFailuresRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBulkProductParamJobFailuresRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBulkProductParamJobFailuresRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
    };
  },

  toJSON(message: ListBulkProductParamJobFailuresRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListBulkProductParamJobFailuresRequest>): ListBulkProductParamJobFailuresRequest {
    return ListBulkProductParamJobFailuresRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBulkProductParamJobFailuresRequest>): ListBulkProductParamJobFailuresRequest {
    const message = createBaseListBulkProductParamJobFailuresRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseListBulkProductParamJobFailuresResponse(): ListBulkProductParamJobFailuresResponse {
  return { base: undefined, failures: [] };
}

export const ListBulkProductParamJobFailuresResponse: MessageFns<ListBulkProductParamJobFailuresResponse> = {
  encode(message: ListBulkProductParamJobFailuresResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    for (const v of message.failures) {
      BulkProductParamJobFailure.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBulkProductParamJobFailuresResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBulkProductParamJobFailuresResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failures.push(BulkProductParamJobFailure.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBulkProductParamJobFailuresResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      failures: globalThis.Array.isArray(object?.failures)
        ? object.failures.map((e: any) => BulkProductParamJobFailure.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListBulkProductParamJobFailuresResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.failures?.length) {
      obj.failures = message.failures.map((e) => BulkProductParamJobFailure.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListBulkProductParamJobFailuresResponse>): ListBulkProductParamJobFailuresResponse {
    return ListBulkProductParamJobFailuresResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBulkProductParamJobFailuresResponse>): ListBulkProductParamJobFailuresResponse {
    const message = createBaseListBulkProductParamJobFailuresResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.failures = object.failures?.map((e) => BulkProductParamJobFailure.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBulkProductParamJobFailure(): BulkProductParamJobFailure {
  return { productSysId: 0, productCode: "", errorMessage: "" };
}

export const BulkProductParamJobFailure: MessageFns<BulkProductParamJobFailure> = {
  encode(message: BulkProductParamJobFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.productSysId !== 0) {
      writer.uint32(8).int64(message.productSysId);
    }
    if (message.productCode !== "") {
      writer.uint32(18).string(message.productCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkProductParamJobFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkProductParamJobFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.productSysId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.productCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkProductParamJobFailure {
    return {
      productSysId: isSet(object.productSysId)
        ? globalThis.Number(object.productSysId)
        : isSet(object.product_sys_id)
        ? globalThis.Number(object.product_sys_id)
        : 0,
      productCode: isSet(object.productCode)
        ? globalThis.String(object.productCode)
        : isSet(object.product_code)
        ? globalThis.String(object.product_code)
        : "",
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: BulkProductParamJobFailure): unknown {
    const obj: any = {};
    if (message.productSysId !== 0) {
      obj.productSysId = Math.round(message.productSysId);
    }
    if (message.productCode !== "") {
      obj.productCode = message.productCode;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create(base?: DeepPartial<BulkProductParamJobFailure>): BulkProductParamJobFailure {
    return BulkProductParamJobFailure.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkProductParamJobFailure>): BulkProductParamJobFailure {
    const message = createBaseBulkProductParamJobFailure();
    message.productSysId = object.productSysId ?? 0;
    message.productCode = object.productCode ?? "";
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

/**
 * CostProductParamBulkService runs asynchronous bulk edits of applicable
 * parameters and param values across many products at once, mirroring
 * MBHeadService's bulk-lifecycle job pattern (submit → job info → poll status
 * → list failures) in yarn_master.proto.
 */
export type CostProductParamBulkServiceDefinition = typeof CostProductParamBulkServiceDefinition;
export const CostProductParamBulkServiceDefinition = {
  name: "CostProductParamBulkService",
  fullName: "finance.v1.CostProductParamBulkService",
  methods: {
    /**
     * BulkEditProductParams applies a list of add-applicable/remove-applicable/
     * upsert-value operations, in order, to up to 500 products in one request.
     * Runs asynchronously via a job: the response returns immediately with job
     * info while the per-product work happens in a background worker, each
     * product in its own transaction — poll GetBulkProductParamJobStatus for
     * progress.
     */
    bulkEditProductParams: {
      name: "BulkEditProductParams",
      requestType: BulkEditProductParamsRequest,
      requestStream: false,
      responseType: BulkEditProductParamsResponse,
      responseStream: false,
      options: {},
    },
    /**
     * GetBulkProductParamJobStatus polls the live status and progress counters
     * of a bulk product-param edit job while it is still running.
     */
    getBulkProductParamJobStatus: {
      name: "GetBulkProductParamJobStatus",
      requestType: GetBulkProductParamJobStatusRequest,
      requestStream: false,
      responseType: GetBulkProductParamJobStatusResponse,
      responseStream: false,
      options: {},
    },
    /**
     * ListBulkProductParamJobFailures lists the per-product failures recorded
     * by a finished (or in-progress) bulk product-param edit job, so the caller
     * can see which products failed and why.
     */
    listBulkProductParamJobFailures: {
      name: "ListBulkProductParamJobFailures",
      requestType: ListBulkProductParamJobFailuresRequest,
      requestStream: false,
      responseType: ListBulkProductParamJobFailuresResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

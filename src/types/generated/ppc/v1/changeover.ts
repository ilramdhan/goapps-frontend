// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: ppc/v1/changeover.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AuditInfo, BaseResponse, PaginationResponse } from "../../common/v1/common";

export const protobufPackage = "ppc.v1";

/** ChangeoverComponent is one applied changeover component (BASE or C1-C7). */
export interface ChangeoverComponent {
  componentId: number;
  eventId: number;
  /** BASE / C1..C7 */
  componentCode: string;
  /** minutes */
  durationApplied: number;
  /** decimal-as-string (kg) */
  wasteApplied: string;
  isAutoDetected: boolean;
  overrideBy: number;
  /** ISO-8601 */
  overrideAt: string;
}

/** ChangeoverEvent is a changeover between two WOs on a machine. */
export interface ChangeoverEvent {
  eventId: number;
  fromWoId: number;
  toWoId: number;
  machineId: number;
  /** denormalized for UI */
  machineNo: string;
  /** minutes */
  durationEstimated: number;
  /** decimal-as-string (kg) */
  wasteEstimated: string;
  /** MINOR / MEDIUM / MAJOR / DEEP */
  group: string;
  durationActual: number;
  /** decimal-as-string (kg) */
  wasteActual: string;
  /** PLANNED / IN_PROGRESS / DONE */
  status: string;
  /** ISO-8601 */
  startedAt: string;
  /** ISO-8601 */
  completedAt: string;
  notes: string;
  components: ChangeoverComponent[];
  audit: AuditInfo | undefined;
}

/** GetChangeoverEvent retrieves a single changeover event with its components. */
export interface GetChangeoverEventRequest {
  eventId: number;
}

export interface GetChangeoverEventResponse {
  base: BaseResponse | undefined;
  data: ChangeoverEvent | undefined;
}

/** ListChangeoverEvents lists changeover events for a machine/date window. */
export interface ListChangeoverEventsRequest {
  page: number;
  pageSize: number;
  machineId?: number | undefined;
  dateFrom: string;
  dateTo: string;
  status: string;
}

export interface ListChangeoverEventsResponse {
  base: BaseResponse | undefined;
  data: ChangeoverEvent[];
  pagination: PaginationResponse | undefined;
}

/**
 * ChangeoverComponentInput is a PPC override line for a create request
 * (add/remove/adjust auto-detected components).
 */
export interface ChangeoverComponentInput {
  componentCode: string;
  /** minutes */
  durationApplied: number;
  /** decimal-as-string (kg) */
  wasteApplied: string;
}

/**
 * DetectChangeover previews the active components (BASE, C1-C7), estimated
 * duration/waste and group for a hypothetical changeover between two WOs.
 * It does NOT persist anything.
 */
export interface DetectChangeoverRequest {
  fromWoId: number;
  toWoId: number;
  /** force C7 deep-clean component */
  deepClean: boolean;
}

export interface DetectChangeoverResponse {
  base: BaseResponse | undefined;
  components: ChangeoverComponent[];
  /** minutes */
  durationEstimated: number;
  /** decimal-as-string (kg) */
  wasteEstimated: string;
  /** MINOR / MEDIUM / MAJOR / DEEP */
  group: string;
}

/**
 * CreateChangeoverEvent persists a planned changeover event. When components
 * are omitted they are auto-detected from the WO spec diff; machine_id = 0
 * resolves to the destination WO's machine.
 */
export interface CreateChangeoverEventRequest {
  fromWoId: number;
  toWoId: number;
  /** 0 = resolve from to_wo */
  machineId: number;
  deepClean: boolean;
  notes: string;
  /** empty = auto-detect */
  components: ChangeoverComponentInput[];
}

export interface CreateChangeoverEventResponse {
  base: BaseResponse | undefined;
  data: ChangeoverEvent | undefined;
}

/** StartChangeover transitions a PLANNED event to IN_PROGRESS. */
export interface StartChangeoverRequest {
  eventId: number;
}

export interface StartChangeoverResponse {
  base: BaseResponse | undefined;
  data: ChangeoverEvent | undefined;
}

/** UpdateChangeoverActual records actual duration/waste and completes the event. */
export interface UpdateChangeoverActualRequest {
  eventId: number;
  /** minutes */
  durationActual: number;
  /** decimal-as-string (kg) */
  wasteActual: string;
  notes: string;
}

export interface UpdateChangeoverActualResponse {
  base: BaseResponse | undefined;
  data: ChangeoverEvent | undefined;
}

function createBaseChangeoverComponent(): ChangeoverComponent {
  return {
    componentId: 0,
    eventId: 0,
    componentCode: "",
    durationApplied: 0,
    wasteApplied: "",
    isAutoDetected: false,
    overrideBy: 0,
    overrideAt: "",
  };
}

export const ChangeoverComponent: MessageFns<ChangeoverComponent> = {
  encode(message: ChangeoverComponent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.componentId !== 0) {
      writer.uint32(8).int64(message.componentId);
    }
    if (message.eventId !== 0) {
      writer.uint32(16).int64(message.eventId);
    }
    if (message.componentCode !== "") {
      writer.uint32(26).string(message.componentCode);
    }
    if (message.durationApplied !== 0) {
      writer.uint32(32).int32(message.durationApplied);
    }
    if (message.wasteApplied !== "") {
      writer.uint32(42).string(message.wasteApplied);
    }
    if (message.isAutoDetected !== false) {
      writer.uint32(48).bool(message.isAutoDetected);
    }
    if (message.overrideBy !== 0) {
      writer.uint32(56).int64(message.overrideBy);
    }
    if (message.overrideAt !== "") {
      writer.uint32(66).string(message.overrideAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeoverComponent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeoverComponent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.componentId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.eventId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.componentCode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.durationApplied = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.wasteApplied = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isAutoDetected = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.overrideBy = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.overrideAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeoverComponent {
    return {
      componentId: isSet(object.componentId)
        ? globalThis.Number(object.componentId)
        : isSet(object.component_id)
        ? globalThis.Number(object.component_id)
        : 0,
      eventId: isSet(object.eventId)
        ? globalThis.Number(object.eventId)
        : isSet(object.event_id)
        ? globalThis.Number(object.event_id)
        : 0,
      componentCode: isSet(object.componentCode)
        ? globalThis.String(object.componentCode)
        : isSet(object.component_code)
        ? globalThis.String(object.component_code)
        : "",
      durationApplied: isSet(object.durationApplied)
        ? globalThis.Number(object.durationApplied)
        : isSet(object.duration_applied)
        ? globalThis.Number(object.duration_applied)
        : 0,
      wasteApplied: isSet(object.wasteApplied)
        ? globalThis.String(object.wasteApplied)
        : isSet(object.waste_applied)
        ? globalThis.String(object.waste_applied)
        : "",
      isAutoDetected: isSet(object.isAutoDetected)
        ? globalThis.Boolean(object.isAutoDetected)
        : isSet(object.is_auto_detected)
        ? globalThis.Boolean(object.is_auto_detected)
        : false,
      overrideBy: isSet(object.overrideBy)
        ? globalThis.Number(object.overrideBy)
        : isSet(object.override_by)
        ? globalThis.Number(object.override_by)
        : 0,
      overrideAt: isSet(object.overrideAt)
        ? globalThis.String(object.overrideAt)
        : isSet(object.override_at)
        ? globalThis.String(object.override_at)
        : "",
    };
  },

  toJSON(message: ChangeoverComponent): unknown {
    const obj: any = {};
    if (message.componentId !== 0) {
      obj.componentId = Math.round(message.componentId);
    }
    if (message.eventId !== 0) {
      obj.eventId = Math.round(message.eventId);
    }
    if (message.componentCode !== "") {
      obj.componentCode = message.componentCode;
    }
    if (message.durationApplied !== 0) {
      obj.durationApplied = Math.round(message.durationApplied);
    }
    if (message.wasteApplied !== "") {
      obj.wasteApplied = message.wasteApplied;
    }
    if (message.isAutoDetected !== false) {
      obj.isAutoDetected = message.isAutoDetected;
    }
    if (message.overrideBy !== 0) {
      obj.overrideBy = Math.round(message.overrideBy);
    }
    if (message.overrideAt !== "") {
      obj.overrideAt = message.overrideAt;
    }
    return obj;
  },

  create(base?: DeepPartial<ChangeoverComponent>): ChangeoverComponent {
    return ChangeoverComponent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChangeoverComponent>): ChangeoverComponent {
    const message = createBaseChangeoverComponent();
    message.componentId = object.componentId ?? 0;
    message.eventId = object.eventId ?? 0;
    message.componentCode = object.componentCode ?? "";
    message.durationApplied = object.durationApplied ?? 0;
    message.wasteApplied = object.wasteApplied ?? "";
    message.isAutoDetected = object.isAutoDetected ?? false;
    message.overrideBy = object.overrideBy ?? 0;
    message.overrideAt = object.overrideAt ?? "";
    return message;
  },
};

function createBaseChangeoverEvent(): ChangeoverEvent {
  return {
    eventId: 0,
    fromWoId: 0,
    toWoId: 0,
    machineId: 0,
    machineNo: "",
    durationEstimated: 0,
    wasteEstimated: "",
    group: "",
    durationActual: 0,
    wasteActual: "",
    status: "",
    startedAt: "",
    completedAt: "",
    notes: "",
    components: [],
    audit: undefined,
  };
}

export const ChangeoverEvent: MessageFns<ChangeoverEvent> = {
  encode(message: ChangeoverEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== 0) {
      writer.uint32(8).int64(message.eventId);
    }
    if (message.fromWoId !== 0) {
      writer.uint32(16).int64(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      writer.uint32(24).int64(message.toWoId);
    }
    if (message.machineId !== 0) {
      writer.uint32(32).int64(message.machineId);
    }
    if (message.machineNo !== "") {
      writer.uint32(42).string(message.machineNo);
    }
    if (message.durationEstimated !== 0) {
      writer.uint32(48).int32(message.durationEstimated);
    }
    if (message.wasteEstimated !== "") {
      writer.uint32(58).string(message.wasteEstimated);
    }
    if (message.group !== "") {
      writer.uint32(66).string(message.group);
    }
    if (message.durationActual !== 0) {
      writer.uint32(72).int32(message.durationActual);
    }
    if (message.wasteActual !== "") {
      writer.uint32(82).string(message.wasteActual);
    }
    if (message.status !== "") {
      writer.uint32(90).string(message.status);
    }
    if (message.startedAt !== "") {
      writer.uint32(98).string(message.startedAt);
    }
    if (message.completedAt !== "") {
      writer.uint32(106).string(message.completedAt);
    }
    if (message.notes !== "") {
      writer.uint32(114).string(message.notes);
    }
    for (const v of message.components) {
      ChangeoverComponent.encode(v!, writer.uint32(122).fork()).join();
    }
    if (message.audit !== undefined) {
      AuditInfo.encode(message.audit, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeoverEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeoverEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fromWoId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.toWoId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.machineId = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.machineNo = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.durationEstimated = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.wasteEstimated = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.group = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.durationActual = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.wasteActual = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.startedAt = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.completedAt = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.components.push(ChangeoverComponent.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.audit = AuditInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeoverEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.Number(object.eventId)
        : isSet(object.event_id)
        ? globalThis.Number(object.event_id)
        : 0,
      fromWoId: isSet(object.fromWoId)
        ? globalThis.Number(object.fromWoId)
        : isSet(object.from_wo_id)
        ? globalThis.Number(object.from_wo_id)
        : 0,
      toWoId: isSet(object.toWoId)
        ? globalThis.Number(object.toWoId)
        : isSet(object.to_wo_id)
        ? globalThis.Number(object.to_wo_id)
        : 0,
      machineId: isSet(object.machineId)
        ? globalThis.Number(object.machineId)
        : isSet(object.machine_id)
        ? globalThis.Number(object.machine_id)
        : 0,
      machineNo: isSet(object.machineNo)
        ? globalThis.String(object.machineNo)
        : isSet(object.machine_no)
        ? globalThis.String(object.machine_no)
        : "",
      durationEstimated: isSet(object.durationEstimated)
        ? globalThis.Number(object.durationEstimated)
        : isSet(object.duration_estimated)
        ? globalThis.Number(object.duration_estimated)
        : 0,
      wasteEstimated: isSet(object.wasteEstimated)
        ? globalThis.String(object.wasteEstimated)
        : isSet(object.waste_estimated)
        ? globalThis.String(object.waste_estimated)
        : "",
      group: isSet(object.group) ? globalThis.String(object.group) : "",
      durationActual: isSet(object.durationActual)
        ? globalThis.Number(object.durationActual)
        : isSet(object.duration_actual)
        ? globalThis.Number(object.duration_actual)
        : 0,
      wasteActual: isSet(object.wasteActual)
        ? globalThis.String(object.wasteActual)
        : isSet(object.waste_actual)
        ? globalThis.String(object.waste_actual)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      startedAt: isSet(object.startedAt)
        ? globalThis.String(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.String(object.started_at)
        : "",
      completedAt: isSet(object.completedAt)
        ? globalThis.String(object.completedAt)
        : isSet(object.completed_at)
        ? globalThis.String(object.completed_at)
        : "",
      notes: isSet(object.notes) ? globalThis.String(object.notes) : "",
      components: globalThis.Array.isArray(object?.components)
        ? object.components.map((e: any) => ChangeoverComponent.fromJSON(e))
        : [],
      audit: isSet(object.audit) ? AuditInfo.fromJSON(object.audit) : undefined,
    };
  },

  toJSON(message: ChangeoverEvent): unknown {
    const obj: any = {};
    if (message.eventId !== 0) {
      obj.eventId = Math.round(message.eventId);
    }
    if (message.fromWoId !== 0) {
      obj.fromWoId = Math.round(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      obj.toWoId = Math.round(message.toWoId);
    }
    if (message.machineId !== 0) {
      obj.machineId = Math.round(message.machineId);
    }
    if (message.machineNo !== "") {
      obj.machineNo = message.machineNo;
    }
    if (message.durationEstimated !== 0) {
      obj.durationEstimated = Math.round(message.durationEstimated);
    }
    if (message.wasteEstimated !== "") {
      obj.wasteEstimated = message.wasteEstimated;
    }
    if (message.group !== "") {
      obj.group = message.group;
    }
    if (message.durationActual !== 0) {
      obj.durationActual = Math.round(message.durationActual);
    }
    if (message.wasteActual !== "") {
      obj.wasteActual = message.wasteActual;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.startedAt !== "") {
      obj.startedAt = message.startedAt;
    }
    if (message.completedAt !== "") {
      obj.completedAt = message.completedAt;
    }
    if (message.notes !== "") {
      obj.notes = message.notes;
    }
    if (message.components?.length) {
      obj.components = message.components.map((e) => ChangeoverComponent.toJSON(e));
    }
    if (message.audit !== undefined) {
      obj.audit = AuditInfo.toJSON(message.audit);
    }
    return obj;
  },

  create(base?: DeepPartial<ChangeoverEvent>): ChangeoverEvent {
    return ChangeoverEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChangeoverEvent>): ChangeoverEvent {
    const message = createBaseChangeoverEvent();
    message.eventId = object.eventId ?? 0;
    message.fromWoId = object.fromWoId ?? 0;
    message.toWoId = object.toWoId ?? 0;
    message.machineId = object.machineId ?? 0;
    message.machineNo = object.machineNo ?? "";
    message.durationEstimated = object.durationEstimated ?? 0;
    message.wasteEstimated = object.wasteEstimated ?? "";
    message.group = object.group ?? "";
    message.durationActual = object.durationActual ?? 0;
    message.wasteActual = object.wasteActual ?? "";
    message.status = object.status ?? "";
    message.startedAt = object.startedAt ?? "";
    message.completedAt = object.completedAt ?? "";
    message.notes = object.notes ?? "";
    message.components = object.components?.map((e) => ChangeoverComponent.fromPartial(e)) || [];
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? AuditInfo.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseGetChangeoverEventRequest(): GetChangeoverEventRequest {
  return { eventId: 0 };
}

export const GetChangeoverEventRequest: MessageFns<GetChangeoverEventRequest> = {
  encode(message: GetChangeoverEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== 0) {
      writer.uint32(8).int64(message.eventId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetChangeoverEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangeoverEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangeoverEventRequest {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.Number(object.eventId)
        : isSet(object.event_id)
        ? globalThis.Number(object.event_id)
        : 0,
    };
  },

  toJSON(message: GetChangeoverEventRequest): unknown {
    const obj: any = {};
    if (message.eventId !== 0) {
      obj.eventId = Math.round(message.eventId);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangeoverEventRequest>): GetChangeoverEventRequest {
    return GetChangeoverEventRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangeoverEventRequest>): GetChangeoverEventRequest {
    const message = createBaseGetChangeoverEventRequest();
    message.eventId = object.eventId ?? 0;
    return message;
  },
};

function createBaseGetChangeoverEventResponse(): GetChangeoverEventResponse {
  return { base: undefined, data: undefined };
}

export const GetChangeoverEventResponse: MessageFns<GetChangeoverEventResponse> = {
  encode(message: GetChangeoverEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      ChangeoverEvent.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetChangeoverEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangeoverEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = ChangeoverEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangeoverEventResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? ChangeoverEvent.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: GetChangeoverEventResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = ChangeoverEvent.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangeoverEventResponse>): GetChangeoverEventResponse {
    return GetChangeoverEventResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangeoverEventResponse>): GetChangeoverEventResponse {
    const message = createBaseGetChangeoverEventResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? ChangeoverEvent.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseListChangeoverEventsRequest(): ListChangeoverEventsRequest {
  return { page: 0, pageSize: 0, machineId: undefined, dateFrom: "", dateTo: "", status: "" };
}

export const ListChangeoverEventsRequest: MessageFns<ListChangeoverEventsRequest> = {
  encode(message: ListChangeoverEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== 0) {
      writer.uint32(8).int32(message.page);
    }
    if (message.pageSize !== 0) {
      writer.uint32(16).int32(message.pageSize);
    }
    if (message.machineId !== undefined) {
      writer.uint32(24).int64(message.machineId);
    }
    if (message.dateFrom !== "") {
      writer.uint32(34).string(message.dateFrom);
    }
    if (message.dateTo !== "") {
      writer.uint32(42).string(message.dateTo);
    }
    if (message.status !== "") {
      writer.uint32(50).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListChangeoverEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListChangeoverEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.machineId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dateFrom = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dateTo = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListChangeoverEventsRequest {
    return {
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      pageSize: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      machineId: isSet(object.machineId)
        ? globalThis.Number(object.machineId)
        : isSet(object.machine_id)
        ? globalThis.Number(object.machine_id)
        : undefined,
      dateFrom: isSet(object.dateFrom)
        ? globalThis.String(object.dateFrom)
        : isSet(object.date_from)
        ? globalThis.String(object.date_from)
        : "",
      dateTo: isSet(object.dateTo)
        ? globalThis.String(object.dateTo)
        : isSet(object.date_to)
        ? globalThis.String(object.date_to)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: ListChangeoverEventsRequest): unknown {
    const obj: any = {};
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.machineId !== undefined) {
      obj.machineId = Math.round(message.machineId);
    }
    if (message.dateFrom !== "") {
      obj.dateFrom = message.dateFrom;
    }
    if (message.dateTo !== "") {
      obj.dateTo = message.dateTo;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<ListChangeoverEventsRequest>): ListChangeoverEventsRequest {
    return ListChangeoverEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListChangeoverEventsRequest>): ListChangeoverEventsRequest {
    const message = createBaseListChangeoverEventsRequest();
    message.page = object.page ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.machineId = object.machineId ?? undefined;
    message.dateFrom = object.dateFrom ?? "";
    message.dateTo = object.dateTo ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseListChangeoverEventsResponse(): ListChangeoverEventsResponse {
  return { base: undefined, data: [], pagination: undefined };
}

export const ListChangeoverEventsResponse: MessageFns<ListChangeoverEventsResponse> = {
  encode(message: ListChangeoverEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    for (const v of message.data) {
      ChangeoverEvent.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.pagination !== undefined) {
      PaginationResponse.encode(message.pagination, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListChangeoverEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListChangeoverEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data.push(ChangeoverEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pagination = PaginationResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListChangeoverEventsResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => ChangeoverEvent.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PaginationResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: ListChangeoverEventsResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data?.length) {
      obj.data = message.data.map((e) => ChangeoverEvent.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<ListChangeoverEventsResponse>): ListChangeoverEventsResponse {
    return ListChangeoverEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListChangeoverEventsResponse>): ListChangeoverEventsResponse {
    const message = createBaseListChangeoverEventsResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = object.data?.map((e) => ChangeoverEvent.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseChangeoverComponentInput(): ChangeoverComponentInput {
  return { componentCode: "", durationApplied: 0, wasteApplied: "" };
}

export const ChangeoverComponentInput: MessageFns<ChangeoverComponentInput> = {
  encode(message: ChangeoverComponentInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.componentCode !== "") {
      writer.uint32(10).string(message.componentCode);
    }
    if (message.durationApplied !== 0) {
      writer.uint32(16).int32(message.durationApplied);
    }
    if (message.wasteApplied !== "") {
      writer.uint32(26).string(message.wasteApplied);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeoverComponentInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeoverComponentInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.componentCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.durationApplied = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.wasteApplied = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeoverComponentInput {
    return {
      componentCode: isSet(object.componentCode)
        ? globalThis.String(object.componentCode)
        : isSet(object.component_code)
        ? globalThis.String(object.component_code)
        : "",
      durationApplied: isSet(object.durationApplied)
        ? globalThis.Number(object.durationApplied)
        : isSet(object.duration_applied)
        ? globalThis.Number(object.duration_applied)
        : 0,
      wasteApplied: isSet(object.wasteApplied)
        ? globalThis.String(object.wasteApplied)
        : isSet(object.waste_applied)
        ? globalThis.String(object.waste_applied)
        : "",
    };
  },

  toJSON(message: ChangeoverComponentInput): unknown {
    const obj: any = {};
    if (message.componentCode !== "") {
      obj.componentCode = message.componentCode;
    }
    if (message.durationApplied !== 0) {
      obj.durationApplied = Math.round(message.durationApplied);
    }
    if (message.wasteApplied !== "") {
      obj.wasteApplied = message.wasteApplied;
    }
    return obj;
  },

  create(base?: DeepPartial<ChangeoverComponentInput>): ChangeoverComponentInput {
    return ChangeoverComponentInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChangeoverComponentInput>): ChangeoverComponentInput {
    const message = createBaseChangeoverComponentInput();
    message.componentCode = object.componentCode ?? "";
    message.durationApplied = object.durationApplied ?? 0;
    message.wasteApplied = object.wasteApplied ?? "";
    return message;
  },
};

function createBaseDetectChangeoverRequest(): DetectChangeoverRequest {
  return { fromWoId: 0, toWoId: 0, deepClean: false };
}

export const DetectChangeoverRequest: MessageFns<DetectChangeoverRequest> = {
  encode(message: DetectChangeoverRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fromWoId !== 0) {
      writer.uint32(8).int64(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      writer.uint32(16).int64(message.toWoId);
    }
    if (message.deepClean !== false) {
      writer.uint32(24).bool(message.deepClean);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectChangeoverRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectChangeoverRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fromWoId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toWoId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deepClean = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectChangeoverRequest {
    return {
      fromWoId: isSet(object.fromWoId)
        ? globalThis.Number(object.fromWoId)
        : isSet(object.from_wo_id)
        ? globalThis.Number(object.from_wo_id)
        : 0,
      toWoId: isSet(object.toWoId)
        ? globalThis.Number(object.toWoId)
        : isSet(object.to_wo_id)
        ? globalThis.Number(object.to_wo_id)
        : 0,
      deepClean: isSet(object.deepClean)
        ? globalThis.Boolean(object.deepClean)
        : isSet(object.deep_clean)
        ? globalThis.Boolean(object.deep_clean)
        : false,
    };
  },

  toJSON(message: DetectChangeoverRequest): unknown {
    const obj: any = {};
    if (message.fromWoId !== 0) {
      obj.fromWoId = Math.round(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      obj.toWoId = Math.round(message.toWoId);
    }
    if (message.deepClean !== false) {
      obj.deepClean = message.deepClean;
    }
    return obj;
  },

  create(base?: DeepPartial<DetectChangeoverRequest>): DetectChangeoverRequest {
    return DetectChangeoverRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DetectChangeoverRequest>): DetectChangeoverRequest {
    const message = createBaseDetectChangeoverRequest();
    message.fromWoId = object.fromWoId ?? 0;
    message.toWoId = object.toWoId ?? 0;
    message.deepClean = object.deepClean ?? false;
    return message;
  },
};

function createBaseDetectChangeoverResponse(): DetectChangeoverResponse {
  return { base: undefined, components: [], durationEstimated: 0, wasteEstimated: "", group: "" };
}

export const DetectChangeoverResponse: MessageFns<DetectChangeoverResponse> = {
  encode(message: DetectChangeoverResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    for (const v of message.components) {
      ChangeoverComponent.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.durationEstimated !== 0) {
      writer.uint32(24).int32(message.durationEstimated);
    }
    if (message.wasteEstimated !== "") {
      writer.uint32(34).string(message.wasteEstimated);
    }
    if (message.group !== "") {
      writer.uint32(42).string(message.group);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectChangeoverResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectChangeoverResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.components.push(ChangeoverComponent.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.durationEstimated = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.wasteEstimated = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.group = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectChangeoverResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      components: globalThis.Array.isArray(object?.components)
        ? object.components.map((e: any) => ChangeoverComponent.fromJSON(e))
        : [],
      durationEstimated: isSet(object.durationEstimated)
        ? globalThis.Number(object.durationEstimated)
        : isSet(object.duration_estimated)
        ? globalThis.Number(object.duration_estimated)
        : 0,
      wasteEstimated: isSet(object.wasteEstimated)
        ? globalThis.String(object.wasteEstimated)
        : isSet(object.waste_estimated)
        ? globalThis.String(object.waste_estimated)
        : "",
      group: isSet(object.group) ? globalThis.String(object.group) : "",
    };
  },

  toJSON(message: DetectChangeoverResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.components?.length) {
      obj.components = message.components.map((e) => ChangeoverComponent.toJSON(e));
    }
    if (message.durationEstimated !== 0) {
      obj.durationEstimated = Math.round(message.durationEstimated);
    }
    if (message.wasteEstimated !== "") {
      obj.wasteEstimated = message.wasteEstimated;
    }
    if (message.group !== "") {
      obj.group = message.group;
    }
    return obj;
  },

  create(base?: DeepPartial<DetectChangeoverResponse>): DetectChangeoverResponse {
    return DetectChangeoverResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DetectChangeoverResponse>): DetectChangeoverResponse {
    const message = createBaseDetectChangeoverResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.components = object.components?.map((e) => ChangeoverComponent.fromPartial(e)) || [];
    message.durationEstimated = object.durationEstimated ?? 0;
    message.wasteEstimated = object.wasteEstimated ?? "";
    message.group = object.group ?? "";
    return message;
  },
};

function createBaseCreateChangeoverEventRequest(): CreateChangeoverEventRequest {
  return { fromWoId: 0, toWoId: 0, machineId: 0, deepClean: false, notes: "", components: [] };
}

export const CreateChangeoverEventRequest: MessageFns<CreateChangeoverEventRequest> = {
  encode(message: CreateChangeoverEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fromWoId !== 0) {
      writer.uint32(8).int64(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      writer.uint32(16).int64(message.toWoId);
    }
    if (message.machineId !== 0) {
      writer.uint32(24).int64(message.machineId);
    }
    if (message.deepClean !== false) {
      writer.uint32(32).bool(message.deepClean);
    }
    if (message.notes !== "") {
      writer.uint32(42).string(message.notes);
    }
    for (const v of message.components) {
      ChangeoverComponentInput.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateChangeoverEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeoverEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fromWoId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toWoId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.machineId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deepClean = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.components.push(ChangeoverComponentInput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeoverEventRequest {
    return {
      fromWoId: isSet(object.fromWoId)
        ? globalThis.Number(object.fromWoId)
        : isSet(object.from_wo_id)
        ? globalThis.Number(object.from_wo_id)
        : 0,
      toWoId: isSet(object.toWoId)
        ? globalThis.Number(object.toWoId)
        : isSet(object.to_wo_id)
        ? globalThis.Number(object.to_wo_id)
        : 0,
      machineId: isSet(object.machineId)
        ? globalThis.Number(object.machineId)
        : isSet(object.machine_id)
        ? globalThis.Number(object.machine_id)
        : 0,
      deepClean: isSet(object.deepClean)
        ? globalThis.Boolean(object.deepClean)
        : isSet(object.deep_clean)
        ? globalThis.Boolean(object.deep_clean)
        : false,
      notes: isSet(object.notes) ? globalThis.String(object.notes) : "",
      components: globalThis.Array.isArray(object?.components)
        ? object.components.map((e: any) => ChangeoverComponentInput.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CreateChangeoverEventRequest): unknown {
    const obj: any = {};
    if (message.fromWoId !== 0) {
      obj.fromWoId = Math.round(message.fromWoId);
    }
    if (message.toWoId !== 0) {
      obj.toWoId = Math.round(message.toWoId);
    }
    if (message.machineId !== 0) {
      obj.machineId = Math.round(message.machineId);
    }
    if (message.deepClean !== false) {
      obj.deepClean = message.deepClean;
    }
    if (message.notes !== "") {
      obj.notes = message.notes;
    }
    if (message.components?.length) {
      obj.components = message.components.map((e) => ChangeoverComponentInput.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeoverEventRequest>): CreateChangeoverEventRequest {
    return CreateChangeoverEventRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeoverEventRequest>): CreateChangeoverEventRequest {
    const message = createBaseCreateChangeoverEventRequest();
    message.fromWoId = object.fromWoId ?? 0;
    message.toWoId = object.toWoId ?? 0;
    message.machineId = object.machineId ?? 0;
    message.deepClean = object.deepClean ?? false;
    message.notes = object.notes ?? "";
    message.components = object.components?.map((e) => ChangeoverComponentInput.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateChangeoverEventResponse(): CreateChangeoverEventResponse {
  return { base: undefined, data: undefined };
}

export const CreateChangeoverEventResponse: MessageFns<CreateChangeoverEventResponse> = {
  encode(message: CreateChangeoverEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      ChangeoverEvent.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateChangeoverEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeoverEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = ChangeoverEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeoverEventResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? ChangeoverEvent.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: CreateChangeoverEventResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = ChangeoverEvent.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeoverEventResponse>): CreateChangeoverEventResponse {
    return CreateChangeoverEventResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeoverEventResponse>): CreateChangeoverEventResponse {
    const message = createBaseCreateChangeoverEventResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? ChangeoverEvent.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseStartChangeoverRequest(): StartChangeoverRequest {
  return { eventId: 0 };
}

export const StartChangeoverRequest: MessageFns<StartChangeoverRequest> = {
  encode(message: StartChangeoverRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== 0) {
      writer.uint32(8).int64(message.eventId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartChangeoverRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartChangeoverRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartChangeoverRequest {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.Number(object.eventId)
        : isSet(object.event_id)
        ? globalThis.Number(object.event_id)
        : 0,
    };
  },

  toJSON(message: StartChangeoverRequest): unknown {
    const obj: any = {};
    if (message.eventId !== 0) {
      obj.eventId = Math.round(message.eventId);
    }
    return obj;
  },

  create(base?: DeepPartial<StartChangeoverRequest>): StartChangeoverRequest {
    return StartChangeoverRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartChangeoverRequest>): StartChangeoverRequest {
    const message = createBaseStartChangeoverRequest();
    message.eventId = object.eventId ?? 0;
    return message;
  },
};

function createBaseStartChangeoverResponse(): StartChangeoverResponse {
  return { base: undefined, data: undefined };
}

export const StartChangeoverResponse: MessageFns<StartChangeoverResponse> = {
  encode(message: StartChangeoverResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      ChangeoverEvent.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartChangeoverResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartChangeoverResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = ChangeoverEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartChangeoverResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? ChangeoverEvent.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: StartChangeoverResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = ChangeoverEvent.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<StartChangeoverResponse>): StartChangeoverResponse {
    return StartChangeoverResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartChangeoverResponse>): StartChangeoverResponse {
    const message = createBaseStartChangeoverResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? ChangeoverEvent.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseUpdateChangeoverActualRequest(): UpdateChangeoverActualRequest {
  return { eventId: 0, durationActual: 0, wasteActual: "", notes: "" };
}

export const UpdateChangeoverActualRequest: MessageFns<UpdateChangeoverActualRequest> = {
  encode(message: UpdateChangeoverActualRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== 0) {
      writer.uint32(8).int64(message.eventId);
    }
    if (message.durationActual !== 0) {
      writer.uint32(16).int32(message.durationActual);
    }
    if (message.wasteActual !== "") {
      writer.uint32(26).string(message.wasteActual);
    }
    if (message.notes !== "") {
      writer.uint32(34).string(message.notes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateChangeoverActualRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateChangeoverActualRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.durationActual = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.wasteActual = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateChangeoverActualRequest {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.Number(object.eventId)
        : isSet(object.event_id)
        ? globalThis.Number(object.event_id)
        : 0,
      durationActual: isSet(object.durationActual)
        ? globalThis.Number(object.durationActual)
        : isSet(object.duration_actual)
        ? globalThis.Number(object.duration_actual)
        : 0,
      wasteActual: isSet(object.wasteActual)
        ? globalThis.String(object.wasteActual)
        : isSet(object.waste_actual)
        ? globalThis.String(object.waste_actual)
        : "",
      notes: isSet(object.notes) ? globalThis.String(object.notes) : "",
    };
  },

  toJSON(message: UpdateChangeoverActualRequest): unknown {
    const obj: any = {};
    if (message.eventId !== 0) {
      obj.eventId = Math.round(message.eventId);
    }
    if (message.durationActual !== 0) {
      obj.durationActual = Math.round(message.durationActual);
    }
    if (message.wasteActual !== "") {
      obj.wasteActual = message.wasteActual;
    }
    if (message.notes !== "") {
      obj.notes = message.notes;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateChangeoverActualRequest>): UpdateChangeoverActualRequest {
    return UpdateChangeoverActualRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateChangeoverActualRequest>): UpdateChangeoverActualRequest {
    const message = createBaseUpdateChangeoverActualRequest();
    message.eventId = object.eventId ?? 0;
    message.durationActual = object.durationActual ?? 0;
    message.wasteActual = object.wasteActual ?? "";
    message.notes = object.notes ?? "";
    return message;
  },
};

function createBaseUpdateChangeoverActualResponse(): UpdateChangeoverActualResponse {
  return { base: undefined, data: undefined };
}

export const UpdateChangeoverActualResponse: MessageFns<UpdateChangeoverActualResponse> = {
  encode(message: UpdateChangeoverActualResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      ChangeoverEvent.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateChangeoverActualResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateChangeoverActualResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = ChangeoverEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateChangeoverActualResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? ChangeoverEvent.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: UpdateChangeoverActualResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = ChangeoverEvent.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateChangeoverActualResponse>): UpdateChangeoverActualResponse {
    return UpdateChangeoverActualResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateChangeoverActualResponse>): UpdateChangeoverActualResponse {
    const message = createBaseUpdateChangeoverActualResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? ChangeoverEvent.fromPartial(object.data)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
